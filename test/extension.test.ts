import * as assert from 'assert';

import * as vscode from 'vscode';
import * as myExtension from '../src/extension';

suite("Vaulty tests", () => {
    test("Something 1", () => {
        assert.equal(1, 1);
    });
});